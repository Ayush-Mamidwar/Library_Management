{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --coverage" 
  },
  "devDependencies": {
    "jest": "^28.0.0", 
    "jest-html-reporters": "^3.0.0" 
  },
  "jest": {
    "reporters": [
      "default"
    ]
  }
}
